# AIPEXBASE 项目创建脚本配置
#
# 使用说明：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 修改 .env 文件中的配置值
# 3. 运行脚本：python create_aipexbase_project.py

# ====================================
# AIPEXBASE 服务器配置
# ====================================
# 服务器基础 URL（必填）
AIPEXBASE_BASE_URL=http://localhost:8080

# ====================================
# 管理员认证信息
# ====================================
# 管理员邮箱（必填）
AIPEXBASE_ADMIN_EMAIL=admin@example.com

# 管理员密码（必填）
AIPEXBASE_ADMIN_PASSWORD=your_password_here

# ====================================
# API Key 配置
# ====================================
# API Key 显示名称（可选，默认：MCP 专用密钥）
AIPEXBASE_API_KEY_NAME=MCP 专用密钥

# API Key 描述（可选，默认：用于 MCP 工具调用的 API 密钥）
AIPEXBASE_API_KEY_DESC=用于 MCP 工具调用的 API 密钥

# API Key 过期时间（可选，留空使用默认值）
# 格式示例：2025-12-31 23:59:59
AIPEXBASE_API_KEY_EXPIRE=

# ====================================
# 输出配置
# ====================================
# MCP 配置文件输出路径（可选，默认：mcp_config.json）
AIPEXBASE_OUTPUT_FILE=mcp_config.json

# 是否显示详细输出（可选，默认：false）
# 可选值：true, false
AIPEXBASE_VERBOSE=false
